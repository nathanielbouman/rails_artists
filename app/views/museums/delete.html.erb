<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="museums delete">
  <h1>Delete Museum</h1>

  <%= form_for(:museum, :url => {:action => 'destroy', :id => @museum.id}) do |f| %>

    <p>Are you sure you want to permanently delete this museum?</p>

    <p class="reference-name"><%= @museum.name %></p>

    <div class="form-buttons">
      <%= submit_tag("Delete Museum") %>
    </div>

  <% end %>
</div>
